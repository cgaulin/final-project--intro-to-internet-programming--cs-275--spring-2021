window.onload = () => {
    let input = window.prompt(`Enter a number`);
    input = parseInt(input, 10);

    function oddDiamond(val){
        let i = 0;
        let looper = 0;
        let shape = '';
    }
};
